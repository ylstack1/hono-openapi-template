version: "3.9"

services:
  backend:
    build: .
    container_name: baas-workers-backend
    ports:
      - "8787:8787"
      - "8788:8788"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: pnpm dev
